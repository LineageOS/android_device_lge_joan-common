allow hwservicemanager init:dir { search };
allow hwservicemanager init:file { open read };
allow hwservicemanager init:process { getattr };

allow hwservicemanager vss_ims:dir { search };
allow hwservicemanager vss_ims:file { open read };
allow hwservicemanager vss_ims:process { getattr };
